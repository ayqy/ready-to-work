import React, { Component } from 'react';
import { Alert } from 'antd';

export default class Setting extends Component {
  render() {
    return (
      <div>
        <Alert message={'Setting Page'} type="success" />
      </div>
    );
  }
}
